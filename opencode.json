{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "external_directory": {
      "/Users/tuned-silicon/code/arrowspace-rs/**": "allow"
    }
  },
  "references": {
    "arrowspace-rs": {
      "path": "../arrowspace-rs",
      "description": "The arrowspace library source tree. The bench dependency resolves from crates.io, so the local tree may be ahead of the published API — verify imports against ~/.cargo/registry/src before using APIs seen only here."
    }
  }
}
